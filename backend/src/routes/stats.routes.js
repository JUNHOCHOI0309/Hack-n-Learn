// 통계 관련 라우트
import { Router } from "express";
const router = Router();

router.get("/problems", () => {});
router.get("/users/:id", () => {});
router.get("/recommedations", () => {});

export default router;