// 풀이 이력 관련 라우트
import { Router } from "express";
const router = Router();

router.get("/", () => {});
router.get("/:userId", () => {});
router.post("/solve/:problemId", () => {});

export default router;