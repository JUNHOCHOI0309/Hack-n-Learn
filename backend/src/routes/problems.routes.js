// 문제 관련 라우트
import { Router } from "express";
const router = Router();

router.get("/", () => {});
router.get("/:id", () => {});
router.post("/", () => {});
router.put("/:id", () => {});
router.delete("/:id", () => {});

export default router;